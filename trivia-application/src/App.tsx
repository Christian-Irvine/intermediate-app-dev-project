import Quiz from "./components/Quiz"

const App: React.FC = () => {
  // const [quizData, setQuizData] = useState(undefined);

  // useEffect(() => {
  //   console.log(quizData);
  // }, [quizData]);

  return (
    <>
      <Quiz/>
    </>
  )
}

export default App
